# Dependencies
node_modules/

# Build output
dist/

# Local environment — never commit secrets
.env
.env.local
.env.*.local

# Vite cache
.vite/
/tmp/vite-cache/

# Test coverage
coverage/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp


# Cypress artifacts
cypress/screenshots/
cypress/videos/
cypress/reports/

# Visual regression (Session 94) -- 'actual' and 'diff' are regenerated on
# every run; 'base' is the committed source of truth for parity diffs and
# must NOT be ignored.
cypress/snapshots/actual/
cypress/snapshots/diff/

# Claude Code agent worktrees -- local scratch copies, not source
.claude/worktrees/